import React from 'react';
import { Tabulation } from '../components/Tabulation';

const FormHistory: React.FC = () => {
  return <Tabulation flow_status={0} pageSize={10}></Tabulation>;
};

export { FormHistory };
